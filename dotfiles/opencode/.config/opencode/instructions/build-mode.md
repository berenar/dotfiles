## Build Mode

Important: never commit or push anything.

When implementing a plan derived from a Linear issue, you should create a branch named like `Issue.branchName` suggests. That info is available when having requested `linear_get_issue`
